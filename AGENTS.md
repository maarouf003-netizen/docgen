# Formatting Rules (Arabic BiDi)

عند الإجابة باللغة العربية:
- ضع المصطلحات الإنكليزية والمتغيرات والمسارات داخل `backticks`
- حافظ على محتوى الكود والأوامر كما هو دون ترجمة

# Coding Standards (Mandatory — apply to every change)

أي تعديل أو إنشاء جديد في هذا الكود **يجب أن يكون وفق الأصول البرمجية القياسية الصارمة** التي لا تقبل أي طرق مختصرة أو حلول اسعافية، لأن المخاطر كبيرة (نظام يعتمد عليه في عمل حقيقي). التزم بما يلي في كل تغيير:

- **لا حلول مختصرة**: اكتب الحل كاملًا وصحيحًا ببنية نظيفة، ولا تستبدل الحلول السليمة بإصلاحات سريعة مؤقتة.
- **الالتزام بالبنية الحالية**: اتبع أنماط المشروع القائمة (المجلدات، التسمية، الاختبارات) دون انحراف.
- **لا تغييرات كاسرة**: لا تُكسِر العقود الحالية (الواجهات، DTOs، نقاط النهاية، سلوك الفروع) إلا بتحديث شامل ومبرر.
- **التحقق الكامل إلزامي** بعد أي تغيير: شغّل الاختبارات (`dotnet test` للخلفية، `npx vitest run` للواجهة)، وأدوات الفحص (`npx oxlint src`, `npx tsc -b`)، والبناء (`npm run build`)، والتأكد من بقاء كل شيء أخضر قبل اعتبار المهمة منجزة.
- **أضف/حدّث الاختبارات** مع كل تغيير سلوكي ولا تُهمل الحالات الحدّية (قيم فارغة، `null`، الفراغ بعد التعبئة، تواريخ حدّية).
- **مراجعة المسار الكامل إلزامية قبل إعلان الإنجاز** — لا تنتظر طلب مراجعة من المستخدم، نفّذ القائمة التالية ذاتيًا بعد كل مهمة:
  1. **تدقيق العقود عبر المسار كاملًا**: تتبع تدفّقًا واحدًا من النهاية للنهاية (واجهة → `DTO` → `Service` → قاعدة → عرض/إحصاءات) وقارن أسماء الحقول وأنواعها وتسمياتها حقلًا بحقل، وخاصة العقود المشتركة بين `frontend/` و`backend/` (أنواع `DateTime?`/`decimal?`/`string` القابلة للفراغ ونحوها).
  2. **فحص بقايا المصطلحات/الأسماء القديمة** عبر البحث الشامل (`grep`/`rg`) عن المعرّفات المستبدلة، والتأكد من أنها لا تظهر إلا في الهجرات التاريخية وملفاتها `Designer` حيث يجب بقاؤها.
  3. **قائمة تحقق مقابل الخطة المعتمدة**: طابق كل بندٍ في المطلوب مع ما نُفذ فعليًا، وقِس أي انحراف.
  4. **التأكد من عدم كسر الاصطلاحات القائمة**: توظيف الدوال/المكوّنات/الصيغ المتعارف عليها (كالتمثيل النقدي) بدل تكرار الأنماط يدويًا.
- **الأمان**: لا تضع أسرارًا أو مفاتيح في الكود، ولا تُثبّت صلاحيات واسعة دون مبرر، واحترم دوال التحقق والتدقيق القائمة.

# Date Fields Rule (Mandatory — apply to every change)

**جميع حقول التواريخ في هذا النظام تكون «تواريخ حرة» بهذا الشكل المعتمد (ولا يُستخدم أي `type="date"`):**

- **النموذج (Frontend)**: حقل نص عادي (`type="text"`) مع `placeholder="مثال: 1/8/2026"`، وتمرّ القيمة عند الإرسال عبر `normalizeArabicDigits(...).trim()` في `DocumentForm` قبل بناء الـ payload.
- **العقد (DTO)**: يُرسَل ويُعاد كـ `string?` (وليس `DateTime?`) في الطلب والاستجابة.
- **الخلفية (Service)**: كل تاريخ يُفسَّر عبر `ParseDateTime(value, "اسم الحقل")` (في `DocumentService`) الذي يحوّله إلى `DateTime?` باستخدام `ActionDateParser` بصيغه السبع: `d/M/yyyy`, `dd/MM/yyyy`, `d-M-yyyy`, `dd-MM-yyyy`, `yyyy-MM-dd`, `d/M/yy`, `dd/MM/yy` + بديل مرن `TryParse`.
  - الترتيب **يوم/شهر/سنة** دائمًا (الرقم الأول يوم والثاني شهر): `1/8/2026` = 1 آب 2026.
  - الفارغ/الأبيض → `null`؛ والقيمة غير الصالحة (غير الفارغة) تُرفض برسالة: `"{اسم الحقل} غير صالح — استخدم مثال: 1/8/2026"`.
- **التخزين (Entity/DB)**: يُخزَّن `DateTime?`؛ والاستجابة تنسّقه `"yyyy-MM-dd"` (InvariantCulture) ليُعرض نصًا، وتُقبل الصيغة ISO عند إعادة الإرسال.
- **حالات لا يتغير فيها الكيان**: إذا كان التخزين الحالي `DateTime?` فلا حاجة لهجرة، إلا إذا تغيّر نوع العمود نفسه.
- **مثال مرجعي مطبّق**: `RegistrationDate` (المتجر المسجل) و`LicenseDate` (المتجر غير المسجل) في `AssetDto` و`ApplicantSideSections.tsx`.


# Frontend Rules (Mandatory — apply to every change)

المشروع يعمل على شاشة الكمبيوتر والجوال معًا، وحوالي نصف الاستخدام من الجوال. لذلك التزم بهذه القواعد في **أي تعديل** على `frontend/`:

- **Mobile-first**: ابنِ أي واجهة أو مكوّن جديد للجوال أولاً ثم أضف نقاط الكسر (`sm:`/`md:`/`lg:`).
- **ممنوع الأبعاد الثابتة** للحاويات/الخطوط التي تسبب تجاوزًا أفقيًا؛ استخدم المرن `flex-wrap`، `min-w-*`, `max-w-*`, `grid` مع `minmax()`.
- **الجداول الواسعة**: على المقاسات الصغيرة حوّلها إلى بطاقات (Cards) كما في `DocumentsList.tsx`، أو على الأقل لفها بـ `overflow-x-auto`.
- **أهداف اللمس**: أي زر/حقل/قائمة تفاعلية بارتفاع أدنى `44px` (`min-h-11`) في المتصفح الجوال.
- **استخدم** `useIsMobile()` (`src/hooks/useMediaQuery.ts`) عند الحاجة لسلوك مختلف جوهريًا بين المقاسين.
- **لا تكسر التنقل**: الشريط الجانبي ثابت على المكتبي، ودرج (Drawer) + شريط تنقل سفلي على الجوال (`Layout.tsx`).
- **التحقق**: بعد أي تعديل على الواجهة، شغّل `npx oxlint src` و `npx tsc -b` و `npx vitest run` و `npm run build`، وأضف/حدّث اختبارات الجوال عند تغيير السلوك.
- **فحص جوال بصري**: عند تغيير يؤثر على التخطيط، تحقّق بصريًا من الشاشات الرئيسية على مقاس جوال (مثلًا 375px) بحثًا عن تجاوز أفقي أو عناصر مقطوعة أو كسر تنقّل، ويفضّل بأتمتة عبر Playwright CLI/DevTools عند توفرها.

# Web Design Guidelines (Mandatory — apply to every UI change)

راجع أي تعديل واجهة مقابل إرشادات Vercel Web Interface Guidelines (المصدر: `https://github.com/vercel-labs/web-interface-guidelines`) — النقاط التالية هي المستخلصة الملزمة هنا:

- **إمكانية الوصول (Accessibility)**: الأزرار الأيقونية بلا نص تحتاج `aria-label`؛ كل حقل نموذج يحتاج `<label>` أو `aria-label`؛ الأزرار بـ `<button>` والتنقل بـ `<a>`/`<Link>` (لا `<div onClick>`)؛ الصور تحتاج `alt` (و`alt=""` للزخرفية)؛ الأيقونات الزخرفية `aria-hidden="true"`؛ العناوين هرمية `<h1>`–`<h6>`.
- **حالة التركيز**: كل عنصر تفاعلي يحتاج حالة تركيز مرئية (`focus-visible:ring-*` أو ما يعادلها)؛ ممنوع `outline-none` بلا بديل تركيز؛ يُفضَّل `:focus-visible` على `:focus`.
- **النماذج**: كل إدخال يحتاج `autocomplete` و`name` ذا معنى؛ استخدم النوع الصحيح (`email`/`tel`/`number`) و`inputmode` المناسب؛ الملصقات قابلة للنقر؛ أخطاء التحقق تُعرض بجانب الحقل ويُركَّز أول خطأ عند الإرسال؛ placeholder يعرض نمطًا مثالًا وينتهي بـ `…`.
- **الحركة (Animation)**: احترم `prefers-reduced-motion`؛ احرّك `transform`/`opacity` فقط؛ ممنوع `transition: all` — اذكر الخصائص صراحة.
- **الطباعة**: `…` لا `...`؛ في أعمدة/مقارنات الأرقام استخدم `tabular-nums`؛ على العناوين `text-wrap: balance` أو `text-pretty`.
- **معالجة المحتوى**: حاويات النصوص الطويلة تحتاج `truncate`/`line-clamp-*`/`break-words`؛ أبناء `flex` يحتاجون `min-w-0` ليعمل القص؛ عالج الحالات الفارغة (لا تعرض واجهة مكسورة لسلاسل/مصفوفات فارغة).
- **الصور**: `<img>` بأبعاد صريحة `width`/`height` (منع CLS)؛ صور أسفل الطيّ `loading="lazy"`.
- **الأداء**: القوائم الكبيرة (>50 عنصرًا) تُرقّمن (virtualize) أو `content-visibility: auto`؛ لا قراءات تخطيط أثناء `render`؛ الحقول ذات التحكّم المحكوم يجب أن تكون رخيصة لكل ضغطة.
- **التنقل والحالة**: الأفعال الخطرة تتطلب تأكيدًا (modal) أو تراجعًا — لا تنفيذ فوري؛ الروابط بـ `<a>`/`<Link>`.
- **اللمس**: `touch-action: manipulation`؛ في المودالات/الأدراج `overscroll-behavior: contain`؛ تجنّب التمرير/الضغط غير المقصود.
- **المناطق الآمنة**: منع الأشرطة غير المرغوبة بإصلاح المحتوى المتجاوز؛ استخدم `flex`/`grid` بدل قياس JS للتخطيط.
- **الأرقام والعملات**: للعرض استخدم `Intl.NumberFormat` (بدل صيغ مكتوبة يدويًا)؛ والاستثناء الوحيد للتواريخ هو قاعدة `Date Fields Rule` أعلاه.
- **ممنوعات (Anti-patterns)**: `user-scalable=no`/`maximum-scale=1`؛ `onPaste`+`preventDefault`؛ `transition: all`؛ `outline-none` بلا بديل؛ `<div>` بنقرة (يجب `<button>`)؛ صور بلا أبعاد؛ مدخلات بلا ملصقات؛ أزرار أيقونات بلا `aria-label`؛ صيغ تاريخ/رقم مكتوبة يدويًا؛ `autoFocus` بلا مبرر.

# Deploy Reminder (Mandatory — apply to every change that touches EF migrations)

هذا النظام يعمل بقاعدتي بيانات: SQLite (`DocGeneratorDbContext`، هجراته في `backend\src\DocGenerator.Infrastructure\Persistence\Migrations`) وPostgreSQL (`DocGeneratorPostgresDbContext`، هجراته في `backend\src\DocGenerator.Infrastructure\Persistence\MigrationsPostgres`). أي تغيير يضيف أو يعدّل هجرات EF **لا يُطبَّق على القواعد الموجودة عند النشر تلقائيًا**، فيجب:

- في تقرير الإنجاز النهائي لأي مهمة تتضمن هجرات جديدة، ذِكر الهجرات بأسمائها وعددها (واحدة لكل سياق عادةً: SQLite + Postgres).
- كتابة تنبيه صريح للمستخدم بوجوب التطبيق في نافذة النشر: `dotnet ef database update --context DocGeneratorDbContext` و`dotnet ef database update --context DocGeneratorPostgresDbContext`.
- عدم اعتبار المهمة «منجزة» قبل أن يُقرّ هذا التنبيه في التقرير، لأن نسيان التطبيق يؤدي لفشل تشغيل فعلي («no such column: …») رغم نجاح الاختبارات محليًا.
- المرجع العملي للمستخدم: `RUN_GUIDE.md` §9 «تطبيق هجرات قاعدة البيانات عند النشر».

