# خطة إعادة هيكلة «مراجعة سجل الجهات العامة»

> **الحالة:** خطة مقترحة بانتظار موافقة صاحب المشروع — لا تُنفَّذ قبل اعتمادها.
> **النطاق:** دمج صفحتي «سجل الجهات العامة» و«سجل تغييرات الجهات» في صفحة واحدة للمدير/المشرف،
> وإعادة بناء «تعديل جهة عامة» على مستوى الهويات الأم (بدون فروع) بثلاثة أفعال: تعديل تسمية، دمج، حلول.
> **قرارات صاحب المشروع المعتمدة في الحوار** (انظر أدناه) — لا تُعدَّل إلا بموافقته.

---

## 1) القرارات المعتمدة (من الحوار — ليست قابلة للتغيير دون موافقة)

1. **الصفحة الموحّدة** «مراجعة سجل الجهات العامة» للمدير والمشرف فقط (`manager`/`admin`).
2. صفحة رئيس القسم «مراجعة سجل الجهات» (`EntityRegistryReview`) **تبقى منفصلة** له كما هي.
3. الصفحة الموحّدة تتضمن **أربعة تبويبات**:
   - `تعديل جهة عامة` (المكوّن الجديد).
   - `إضافة جهة` (نقل نموذج الإضافة القائم من صفحة الإدارة المستقلة).
   - `توحيد تسميات الجهات العامة` — **معطّل** لأنه مؤجل للمرحلة القادمة.
   - `سجل تغييرات الجهة العامة` — تضمين شاشة `EntityChangeLog` الحالية.
4. **`تعديل جهة عامة`** يعمل على مستوى **الهويات الأم** (`PublicEntityGroup`) فقط (بدون الفروع).
5. حقل **بحث واحد** قابل لإضافة عدة هيئات أم إلى «قائمة الجهات المختارة» (اختيار متعدد).
6. زر **«تعديل» بلون أحمر** يفتح قائمة منسدلة بثلاثة أفعال: `تعديل تسمية` / `دمج` / `حلول`.
7. **تعديل تسمية** = فعل **أحادي** للجهة الواحدة: اختيار نوع المرجع (قرار / قانون / مرسوم) +
   حقل «التسمية الجديدة» + رقم المرجع + تاريخه.
8. **دمج** = يمكن ضمّ **عدة** هيئات أم مختارة في هيئة أم واحدة ناجية (تُنتقى)، مع إمكانية إدخال
   **اسم نهائي جديد** للنتيجة (بموجب المرجع) + رقم المرجع + تاريخه.
9. **حلول** = إلغاء **عدة** هيئات أم مختارة واستبدالها بهيئة أم **جديدة** تحلّ محلها، عبر زر
   «إضافة الجهة العامة الجديدة التي حلت محلها» (نموذج إضافة جهة) + رقم المرجع + تاريخه.
10. بيانات المرجع (النوع + الرقم + التاريخ) **إلزامية** في الأفعال الثلاثة (موافقة مواصفة
    `docs/ENTITY_MERGE_MOVE_SPEC.md` §0.4).
11. **التنبيه العام** «لكل المحامين» يعني كل محامي الفروع النشطة عبر كل الفروع (توسيع آلية
    `HeadAlert` التي كانت تبثّ لكل فرع على حدة).
12. كل فعل يبثّ: تنبيه عام لكل المحامين + تنبيه خاص لرؤساء الأقسام + وقوعة ملف (`entity-change`)
    على كل ملف متأثر + سطر في سجل تغييرات الجهة بالفاعل (مدير/مشرف) وبيانات المرجع.

### ملاحظات التصميم المستخلصة
- `تعديل تسمية` يعمل على **جهة أم واحدة** لأنه لكل جهة اسمها الجديد؛ أما `دمج` و`حلول` فيدعمان التعدد.
- نص الوقوعات/التنبيهات القياسي (يُبسّط بالتفصيل في التنفيذ):
  - تعديل تسمية: «تم تعديل اسم الجهة من {X} إلى {Y} بموجب {المرجع}».
  - دمج: «تم دمج {الأسماء} مع {الهدف} بموجب {المرجع}».
  - حلول: «حلّت الجهة {Y} محل {الأسماء} بموجب {المرجع}».

---

## 2) فهم البنية الحالية (أساس التنفيذ)

### التنظيم الحالي للواجهة
| البند في النافذة الجانبية | المسار | المكوّن | الصلاحية الحالية |
|---|---|---|---|
| «سجل الجهات العامة» | `/entities/registry` | `EntityRegistryManagement` | مدير/مشرف/رئيس قسم |
| «مراجعة سجل الجهات» (رئيس القسم) | `/entities/review` | `EntityRegistryReview` | مدير/مشرف/رئيس قسم |
| «سجل تغييرات الجهات» | `/entity-change-log` | `EntityChangeLog` | مدير/مشرف فقط |

- `App.tsx`: مسارات التوجيه (`RequireRole`).
- `Layout.tsx` (أسطر ~125-131): بناء `navItems` + `canManageEntityRegistry` و`hasFullAccess`.

### نموذج البيانات (النقاط المهمة للخطة)
- `PublicEntityGroup` (الهوية الأم): `Id`, `CanonicalName`, `EntityType`, `IsActive`, `CreatedAt`, `Entries`.
- `PublicEntity` (القيد: محافظة + فرع): `Id`, `GroupId`, `Governorate`, `BranchName`,
  `IsParentEntity`, `CoverageLabel?`, `CitationFormula`, `Status`, `IsActive`, `NeedsReview`, …
  - «الجهة الأم» بلا فرع تُرمَّز بـ `BranchName = "الجهة الأم"` (ثابت `DefaultBranchName`) أو
    `IsParentEntity = true`.
- روابط الملفات:
  - `ApplicantPublicEntity.RegistryId?` / `ExecutedPublicEntity.RegistryId?` ← `PublicEntity.Id` (`OnDelete SetNull`).
  - `Document.ApplicantRegistryId?` (نسخة تسريع لفلترة الطالب).
  - الأعمدة النصية `ApplicantPublicEntity.Name`, `ExecutedPublicEntity.EntityName`, `Document.Applicant`,
    `SearchText`, `FullData` تُعاد كتابتها عند إعادة التسمية/الطي.
- `PublicEntityChangeEvent`: `EntryId?`, `GroupId?`, `ActionKind`, `DecreeKind?`, `DecreeNumber?`,
  `DecreeDate?`, `PayloadJson`, `ActorUserId`, `CreatedAtUtc`.
  - `ActionKindCatalog` يملك قيم `rename/merge/abolish/create/...` — لكن **لا يوجد كاتب لـ `abolish`** بعد.
- `HeadAlert` / `HeadAlertRecipient`: البثّ لكل فرع على حدة (`BranchId` واحد + مستلمون).
- `DocumentOccurrence`: نوع `entity-change` (تغيير جهة) يُكتب آليًا على الملفات المتأثرة.

### الفجوات التي سيسدها التنفيذ
1. الـ `rename` الحالي (`UpdateAsync`) يعمل على مستوى **قيدٍ واحد** ولا يبثّ تنبيهًا عامًا، ولا يكتب
   وقوعات ملف (فقط إشعار المُدخِل أثناء المراجعة). نحتاج فعلًا على مستوى **مجموعة** مع بثّ عام + وقوعات.
2. لا يوجد `abolish` فعّال ولا تدفّق «إلغاء + استبدال» متعدد.
3. الدمج الحالي (`CommitMergeAsync`) لا يحمل بيانات المرسوم في طلبه ولا اسمًا نهائيًا جديدًا،
   ويبثّ لرؤساء المحافظات المتأثرة فقط (لا تنبيه عام لكل المحامين).
4. لا يوجد «بثّ عام لكل المحامين في كل الفروع» — يجب توسيع آلية التنبيهات.

---

## 3) خطوات التنفيذ

### المرحلة أ — الخلفية (`backend/`)

#### أ1. توسيع بثّ التنبيهات لكل الفروع
- إضافة سرد **كل الفروع النشطة** و**كل المحامين/رؤساء الأقسام النشطين عبر كل الفروع** إلى
  `IHeadAlertRepository` / `HeadAlertRepository` (أنماط `ListActiveLawyersAsync`/`ListActiveHeadsAsync` القائمة).
- في `PublicEntityService` دالتان خاصتان:
  - `BroadcastEntityChangeToAllLawyersAsync(message, token)`: تنشئ `HeadAlert` لكل فرع بجميع محاميه
    (`TargetType = Branch`) بنصٍ مقطوع عند 2000 حرف (نمط `InsertRenameNoticeToCreatorAsync`).
  - `BroadcastToAllHeadsAsync(message, token)`: تنشئ `HeadAlert` لكل فرع برؤساء أقسامه.

#### أ2. تعزيز إعادة التسمية للجهة الأم (على مستوى المجموعة)
- نقطة نهاية جديدة: `PUT /api/entity-registry/groups/{groupId}/rename`
  الجسم: `{ newCanonicalName, decreeKind, decreeNumber, decreeDate }` (مرجع إلزامي).
- خدمة `RenameGroupAsync` (معاملة واحدة):
  1. تحقق `RolePermissions.HasFullAccess` (مدير/مشرف) — لا رئيس قسم.
  2. تحقق الاسم الجديد فريد وبيانات المرجع مكتملة (نوع + رقم + تاريخ).
  3. تحديث `Group.CanonicalName` + `SyncTextsAfterRenameAsync(old, new)` لكل الملفات المرتبطة.
  4. `PublicEntityChangeEvent` (`ActionKind=Rename` + مرسوم + payload).
  5. وقوعات `entity-change` لكل ملف متأثر: «تم تعديل اسم الجهة من X إلى Y بموجب …».
  6. تنبيه عام لكل المحامين + تنبيه خاص لرؤساء الأقسام.
  7. تدقيق `rename_public_entity_group`.
- نقطة النهاية والخدمة مبنية بالتوازي (لا نعطّل `UpdateAsync` القائمة التي تخدم رئيس القسم).

#### أ3. الدمج المتعدد (توسيع)
- توسيع `MergeCommitRequest` بحقول اختيارية: `NewCanonicalName?`, `DecreeKind`, `DecreeNumber`, `DecreeDate`.
- في `CommitMergeAsync`:
  - إن وُرد `NewCanonicalName`: حدّث `Group.CanonicalName` للهدف ثم `SyncTextsAfterRenameAsync` على الناجي.
  - ثبّت بيانات المرجع في الحدث (ActionKind=merge + مرسوم).
  - نص الوقوعة/التنبيه العام: «تم دمج {الأسماء} مع {الهدف} بموجب …» + تنبيه عام لكل المحامين + رؤساء الأقسام.
  - الحواجز القائمة (لا `NeedsReview`، منع الذات، تأكيد اسم الناجي) تُحفظ.

#### أ4. الحلول (إلغاء عدة + استبدال) — جديد
- نقطة نهاية جديدة: `POST /api/entity-registry/groups/abolish-and-replace`
  الجسم: `{ abolishedGroupIds[], newCanonicalName, entityType, governorate, branchName,
  citationFormula?, coverageLabel?, aliases?, decreeKind, decreeNumber, decreeDate }`.
- خدمة `AbolishAndReplaceAsync` (معاملة واحدة):
  1. تحقق صلاحية مدير/مشرف؛ كل الجهات المُلغاة نشطة بلا `NeedsReview`؛ الاسم الجديد فريد؛ المرجع مكتمل.
  2. إنشاء هيئة أم جديدة + قيدها الأم (`IsParentEntity=true`, `Status=Final`, بلا `NeedsReview`).
  3. لكل قيد فعّال في الجهات المُلغاة: ترحيل `RegistryId` (طالب/منفذ + `Document.ApplicantRegistryId`)
     إلى قيد الجهة الجديدة، وإيقاف القيد المُلغى (`IsActive=false`)، وإيقاف المجموعات المُلغاة
     (`Group.IsActive=false`).
  4. `SyncTextsAfterFoldAsync` لكل الملفات المتأثرة (يحلّ الاسم الجديد محل القديم).
  5. `PublicEntityChangeEvent` (`ActionKind=Abolish` + مرسوم + payload كامل).
  6. وقوعات `entity-change` لكل ملف: «حلّت الجهة {Y} محل {X} بموجب …».
  7. تنبيه عام لكل المحامين + رؤساء الأقسام.
  8. تدقيق `abolish_and_replace_entity`.

#### أ5. الحوكمة والنطاق
- كل نقاط النهاية الجديدة نحوّل عبر `RolePermissions.HasFullAccess` (مدير/مشرف).
- `EntityRegistryController` تحميها بـ `[Authorize]` + فحص الصلاحية مع `Forbid()`.

### المرحلة ب — الواجهة (`frontend/`)

#### ب1. إعادة هيكلة الصفحة والتوجيه
- مكوّن/صفحة موحّدة باسم `EntityRegistryReviewManagement.tsx` (بند القائمة: «مراجعة سجل الجهات العامة`)
  بتبويبات: `تعديل جهة عامة` / `إضافة جهة` / `توحيد تسميات الجهات العامة` (معطّل) / `سجل تغييرات الجهة`.
- `App.tsx`: مسار جديد `RequireRole allowed={(_r, full) => full}` (مدير/مشرف).
- `Layout.tsx`: استبدال بندي «سجل الجهات العامة» و«سجل تغييرات الجهات» ببند واحد للمدير/المشرف؛
  يُحافظ على بند رئيس القسم «مراجعة سجل الجهات» (`/entities/review`).

#### ب2. مكوّن «تعديل جهة عامة» (جديد)
- بحث عن الهويات الأم عبر `/entity-registry/groups?q=...` + إضافة المختار إلى قائمة (بحث متكرر).
- قائمة «الجهات المختارة» (عرض + إزالة).
- زر «تعديل» أحمر → قائمة منسدلة (تعديل تسمية / دمج / حلول).
- **تعديل تسمية** (لجهة واحدة): قائمة (بموجب قرار/قانون/مرسوم) → حقل «التسمية الجديدة» + رقم + تاريخ.
- **دمج** (متعدد → هدف): اختيار الهدف من بحث ثانٍ، حقل «الاسم النهائي» اختياري + قائمة المرجع + رقم + تاريخ.
- **حلول** (متعدد → إضافة جهة): زر «إضافة الجهة العامة الجديدة التي حلت محلها» يفتح نموذج إضافة جهة +
  قائمة المرجع + رقم + تاريخ.
- كل حقول التاريخ `type="text"` بـ `placeholder="مثال: 1/8/2026"` وتمرير `normalizeArabicDigits(...).trim()`
  (قاعدة التواريخ الحرة — أنظر `AGENTS.md`).

#### ب3. التوثيق والتصميم (وفق `AGENTS.md` + إرشادات Vercel)
- `aria-label` على أزرار الأيقونات، `<label>` أو `aria-label` لكل حقل، أزرار بـ `<button>`/`<a>`،
  حالة تركيز `focus-visible:ring-*`، `tabular-nums` للسجلات، `Intl.NumberFormat` للأرقام،
  معالجة الحالات الفارغة، أهداف لمس ≥ `min-h-11`، موبايل أولاً، `prefers-reduced-motion`.

### المرحلة ج — الاختبارات والتحقق

#### الاختبارات (Backend — `DocGenerator.Application.Tests`)
- `RenameGroupAsync`: مزامنة النصوص + مرسوم + تنبيهات (عام + رؤساء) + وقوعات + حدث `rename`.
- الدمج المتعدد + اسم نهائي + مرسوم إلزامي.
- `AbolishAndReplaceAsync`:
  - ترحيل روابط لعدة جهات مُلغاة إلى جهة جديدة واحدة.
  - حدث `ActionKind=Abolish` + مرسوم + وقوعات + تنبيهات.
  - منع `head` وغير المخوّل (`Forbid`/`UnauthorizedAccessException`).
  - إلزامية المرسوم (رفض عند نقص النوع/الرقم/التاريخ).
- تحديث اختبارات الصلاحيات (`EntityRegistryPermissionTests`).

#### الاختبارات (Frontend — `npx vitest run`)
- فلوانب التبويبات الأربعة.
- البحث المتعدد + القائمة المختارة + خطوات `تعديل تسمية/دمج/حلول`.
- التبويب «توحيد تسميات» معطّل (غير قابل للنقر بنص توضيحي).
- تضمين سجل التغييرات.
- تحديث/نقل اختبارات `EntityRegistryManagement.test.tsx` و`EntityChangeLog`.

#### أوامر التحقق الإلزامية (قبل إعلان الإنجاز)
```
dotnet test
npx oxlint src
npx tsc -b
npx vitest run
npm run build
```
- فحص جوال بصري (375px) للصفحة الموحّدة بحثًا عن تجاوز أفقي/عناصر مقطوعة.

---

## 4) الهجرات والنشر

- **غير متوقع الآن**: لا نغيّر جداول أو أعمدة قائمة (نعيد استخدام `PublicEntityChangeEvent`,
  `HeadAlert`, `DocumentOccurrence`, `Group.IsActive`).
- **إن ظهرت حاجة لعمود/Hجرات أثناء التنفيذ**:
  - يتم توليد هجرتين (واحدة لكل سياق: `DocGeneratorDbContext` في
    `backend/src/DocGenerator.Infrastructure/Persistence/Migrations`
    و`DocGeneratorPostgresDbContext` في
    `backend/src/DocGenerator.Infrastructure/Persistence/MigrationsPostgres`).
  - تُذكر أسماؤها وعددها في تقرير الإنجاز، مع تنبيه صريح بوجوب تطبيقها في نافذة النشر:
    ```
    dotnet ef database update --context DocGeneratorDbContext
    dotnet ef database update --context DocGeneratorPostgresDbContext
    ```
  - لا تُعتبر المهمة منجزة قبل إقرار هذا التنبيه في التقرير (المرجع: `RUN_GUIDE.md §9`).

---

## 5) بنود خارج النطاق (مؤجلة)
- `توحيد تسميات الجهات العامة`: تُعيد هيكلته في **المرحلة القادمة** (يُعرَض التبويب معطّلًا الآن).
- أي إحصاءات متقدمة للجهات (المرحلة الرابعة في `docs/ENTITY_PORTAL_PLAN.md`).

---

## 6) مسار مراجعة ما قبل الإنجاز (حسب `AGENTS.md`)
1. تدقيق العقود عبر المسار كاملًا (Frontend → DTO → Service → قاعدة → عرض) حقلًا بحقل.
2. فحص بقايا المصطلحات/الأسماء القديمة عبر `grep`/`rg`.
3. مطابقة كل بند في هذه الخطة مع ما نُفّذ فعلًا، وقياس أي انحراف.
4. عدم كسر الاصطلاحات القائمة (التمثيل النقدي، التواريخ الحرة، أنماط المشروع).
5. الأمان: لا أسرار/مفاتيح، احترام دوال التحقق والتدقيق القائمة.

---

## 7) ثغرات / تعارضات / تسريبات يجب سدّها قبل الاعتماد

> هذا البند مستقل لإعادة المراجعة الشاملة — كل نقطة فيه **إلزامية** قبل اعتبار الخطة معتمدة للتنفيذ.
> المرجع السطري لكل نقطة يتيح التحقق المباشر من الكود.

### 7-أ) تعديل تسمية — تعارض أحادي/متعدد
- **الوصف الأصلي:** «بعد اختيار جهة عامة من الاقتراحات يكون هناك زر تعديل (لونه احمر) وهو قائمة
  منسدلة فيها (تعديل تسمية، دمج، حلول)» — صياغة مفردة، لكن التوضيح اللاحق «يمكن دمج عدة جهات بجهة
  واحدة، كذلك حلول جهة جديدة محل عدة جهات ملغاة» + قرار «قائمة اختيار متعددة من بحث واحد» يجعل
  الواجهة متعددة الاختيار.
- **التعارض:** `تعديل تسمية` فعل أحادي بطبيعته (لكل جهة اسمها الجديد `newCanonicalName` واحد) بينما
  `دمج` و`حلول` متعددان. `PublicEntityService.cs:476` (`UpdateAsync`) و`RenameGroupAsync` المقترح يعملان
  على `groupId` واحد ويفحصان `EnsureCanonicalAvailableAsync`.
- **التسريب إن لم يُسد:** اختيار 3 جهات ثم ضغط `تعديل تسمية` بلا تحديد أيٍّ منها سيُطبّق الاسم الجديد
  على أول عنصر فقط أو يفشل بصمت، ويسجل حدثًا ناقصًا في `PublicEntityChangeEvent`.
- **الإلزام قبل الاعتماد:** تعطيل/إخفاء خيار `تعديل تسمية` عند `selected.length !== 1` مع رسالة
  «اختر جهة واحدة لتعديل تسميتها» (نمط `MergeModal.tsx:59` في فحص `survivorId === ''`)، أو إظهار
  حقل `التسمية الجديدة` لكل عنصر مختار على حدة.

### 7-ب) الفصل بين مستوى `Group` ومستوى `Entry`
- **الوصف الأصلي:** «للجهات العامة (الام) بدون فروع الجهات العامة» — أي الهوية الأم حصرًا.
- **التعارض مع الكود:** الخطة تقترح `PUT /groups/{groupId}/rename` جديد، وهو صحيح، لكنها لم تنص أن
  إعادة التسمية لا تمس `Governorate/BranchName/CitationFormula` — فقط `Group.CanonicalName`.
  للجهة الأم `BranchName = "الجهة الأم"` (`PublicEntityService.cs:92` و`CreateAsync:374`) و`IsParentEntity = true`.
- **التسريب إن لم يُسد:** تعديل `Governorate` لهوية أم مع عدة قيود بمحافظات مختلفة عبر `UpdateAsync:529`
  (`EnsureNoDuplicateEntryAsync`) سيُنشئ تعارض فهرس فريد أو يشتت قيود المحافظات الأخرى.
- **الإلزام قبل الاعتماد:** النص صراحة: إعادة التسمية على مستوى المجموعة لا تغيّر `Governorate/BranchName/
  CitationFormula/CoverageLabel` — فقط الاسم. ورفض أي `group` فيه `Entries.Any(e => e.NeedsReview)`
  كما يفعل `CommitMergeAsync:1599` (وإلا سيُزامن نصوص قيود بانتظار المراجعة لم تظهر بعد للمندوبين
  لأن `PortalRepository` يستبعد `NeedsReview` — مواءمة `docs/ENTITY_PORTAL_PLAN.md §6bis`).

### 7-ج) البحث "الجهات الأم بدون الفروع" — تصويب دقيق بعد مراجعة الكود
- **الوصف الأصلي:** «تظهر الجهات الام بدون الفروع» في حقل البحث.
- **التصويب العلمي (بعد مراجعة الكود):** الزعم السابق أن «`ListGroupsAsync` يعيد كل الهويات دون فلترة
  `IsActive`» **غير دقيق** — `PublicEntityService.cs:188` يطبّق بالفعل `.Where(g => g.IsActive)`. إلا أن
  **جوهر الثغرة يبقى قائمًا وحقيقيًا** لأن الفلترة الناقصة الحقيقية هي ثلاثة أمور مستقلة:
  1. **لا `excludeIds = selectedIds`**: بحث الدمج سيعرض الناجية نفسها بين نتائج الهدف، فيمر الفحص الخاطئ
     `AbsorbedGroupIds.Contains(SurvivorGroupId)` ويفشل بلا رسالة واضحة، أو يسمح باختيار جهة غير نشطة.
  2. **لا حماية من الاختيار المتبادل**: يمكن اختيار الجهة «ب» هدفًا (ناجية) فيما هي نفسها مُختارة للمحو
     في قائمة أخرى، فيتعارض الفحص دون تنبيه.
  3. **نطاق رئيس القسم** (`head`): `ListGroupsAsync:189` يحصر بالمحافظة، لكن البحث في الصفحة الموحّدة
     مخصص **للمدير/المشرف فقط** فلا يُستخدم هذا النطاق أصلاً، فلا حاجة لتقييده هنا.
- **الإلزام قبل الاعتماد:** إضافة معامل `excludeIds = selectedIds` في `GET /groups` لبحث الهدف + حظر
  الاختيار المتبادل (جهة لا تظهر كهدف إن كانت ضمن المختارين للمحو)، والبحث موحّد عبر
  `ArabicNameNormalizer.Normalize` (موجود أساسًا في `ListGroupsAsync:192`). يُغني هذا البند عن التحسين 4
  ويُدمج به (منع الازدواج).

### 7-د) حلول — تفاصيل الجهة الجديدة الناقصة
- **الوصف الأصلي:** «زر إضافة الجهة العامة الجديدة التي حلت محلها وهذا الزر نفس زر إضافة جهة عامة».
- **التعارض:** الخطة تنص على حقول `entityType/governorate/branchName/citationFormula/coverageLabel` للجديدة،
  لكن للجهة الأم `governorate` هو مقرها الإداري و`branchName` يجب أن يُجبر `الجهة الأم` + `IsParentEntity = true`.
  `EntryExistsAsync:25` يمنع التكرار على `(groupId, governorate, branchName)` — قيد أم بلا محافظة يكسر الفهرس.
- **التسريب إن لم يُسد:** إنشاء جهة جديدة في حلول بلا `governorate` أو بـ `branchName` فرعي سيُنشئ قيدًا فرعيًا
  بدل هوية أم، فيظهر في بحث الفروع ولا يُعثر عليه في بحث الأم، وتفشل الوقوعات.
- **الإلزام قبل الاعتماد:** النص: الجهة الجديدة في حلول تُنشأ حصرًا كـ `IsParentEntity = true` و
  `branchName = "الجهة الأم"` (أو تُستمد محافظتها من أول جهة ملغاة إن لم يحددها المدير)، مع تمرير
  `ValidateCoverageLabel` وإلزام `entityType/governorate`.

### 7-هـ) الدمج مع اسم نهائي جديد — ترتيب العمليات
- **الوصف الأصلي + التوضيح:** «نعم، يُدخل اسم نهائي جديد» للنتيجة بعد الدمج.
- **التعارض مع الكود:** الخطة: "إن ورد `NewCanonicalName` حدّث `Group.CanonicalName` للهدف ثم
  `SyncTextsAfterRenameAsync`". لكن `CommitMergeAsync:1578` الحالي يرحّل `RegistryId` أولاً ثم
  `SyncTextsAfterFoldAsync:1697`. إذا غيّرنا اسم الناجية **وبعده** رحّلنا روابط المُمتص، فـ
  `SyncTextsAfterRenameAsync:965` سيبحث عن نصوص مطابقة للاسم القديم للناجية فقط ولن يجد نصوص المُلغى.
- **التسريب إن لم يُسد:** ملفات الجهات المُمتصة تبقى بنصوصها القديمة في `ApplicantPublicEntity.Name`
  و`ExecutedPublicEntity.EntityName` و`SearchText` دون تحديث.
- **الإلزام قبل الاعتماد:** تحديد الترتيب: **1) تغيير اسم الناجية + مزامنة نصوص الناجية، 2) ترحيل روابط
  المُمتص + مزامنة طيّ نهائية بـ `newCanonical` الأخير**، أو جمعهما في مزامنة واحدة نهائية داخل نفس المعاملة
  `_tx.RunAsync`.

### 7-و) البث العام — `HeadAlert` لكل فرع
- **الوصف الأصلي:** «توجيه تنبيه عام لكل المحامين بهذا التعديل» و«تنبيه خاص برؤساء الأقسام».
- **التعارض:** `HeadAlertService.cs:193` يعمل لكل فرع على حدة، و`PublicEntityService.cs:96` يملك
  `IRepository<Branch> _branches` لكن `HeadAlertRepository.cs:54` يملك فقط `ListActiveLawyersAsync(branchId)`.
  الخطة تقترح `BroadcastToAllLawyersAsync` لكنها لم تضف `ListAllActiveLawyersGroupedByBranchAsync`.
- **التسريب إن لم يُسد:** `N+1` استعلام بعدد الفروع، أو تسجيل `CreatedById = head.Id` بدل `actor.UserId`
  كما في `InsertEntryReviewAlertsAsync:434` (الذي يسجل `CreatedById = head.Id`) بينما الدمج الصحيح
  `CommitMergeAsync:1746` يسجل `CreatedById = actor.UserId` — تسريب هوية الفاعل.
- **الإلزام قبل الاعتماد:** إضافة `ListAllActiveLawyersGroupedByBranchAsync` / `ListAllActiveHeadsGroupedByBranchAsync`
  وسقف الرسالة 2000 (`HeadAlertService.cs:99`)، والنص صراحة: كل `HeadAlert` يُنشأ بـ `CreatedById = actor.UserId`
  و`TargetType = Branch` و`BranchId = branch.Id`.

### 7-ز) نطاق المندوبين بعد الإلغاء/الدمج
- **الوصف الأصلي:** لم يذكر مصير مندوبي الجهات الملغاة/المُدمجة، لكن `docs/ENTITY_PORTAL_PLAN.md:9`
  يحدد نطاق المندوب `PortalGroupId/PortalEntryId` (`User`).
- **التعارض:** `CommitMergeAsync` يرحّل `RegistryId` للملفات لكنه لا يرحّل `User.PortalGroupId/PortalEntryId`.
  بعد `AbolishAndReplaceAsync` سيصبح مندوبو الجهات الملغاة بلا نطاق ظاهر (404 في `PortalRepository.ScopePredicate`).
- **التسريب إن لم يُسد:** مندوب جهة ملغاة يفقد الوصول دون إشعار، أو يبقى مرتبطًا بمجموعة `IsActive = false`.
- **الإلزام قبل الاعتماد:** إضافة خطوة في كل من `CommitMergeAsync` الموسع و`AbolishAndReplaceAsync`:
  إعادة توجيه `User` المرتبطين بالمجموعات الملغاة/المُمتصة إلى الجديدة (أو إيقافهم مع تنبيه)، ضمن نفس المعاملة.

### 7-ح) التوجيه والبقايا — تسريب صلاحية عبر URL
- **الوصف الأصلي:** «لا داعي لوجود سجل الجهات العامة وسجل تغييرات الجهات العامة كصفحتين منفصلتين».
- **التعارض:** `App.tsx:130` و`Layout.tsx:125,131` يبنيان المسارات على `hasFullAccess || isHead`. الخطة تقول
  "استبدال بندي سجل الجهات العامة وسجل تغييرات الجهات ببند واحد للمدير/المشرف" لكنها لم تنص على حذف
  `Route /entities/registry` و`/entity-change-log` أو تحويلهما.
- **التسريب إن لم يُسد:** الوصول المباشر عبر URL القديم سيظل ممكنًا (بقايا `grep` لصفحتين قديمتين)، ورئيس القسم
  قد يصل لصفحة موحدة مخصصة للمدير/المشرف.
- **الإلزام قبل الاعتماد:** حذف أو تحويل `Route /entities/registry → Navigate to /entities/review-management?tab=add`
  و`/entity-change-log → ?tab=log`، وتحديث `Layout.tsx:125,131` إلى `if (hasFullAccess) navItems.push(...)` للبند الموحد،
  مع الإبقاء على `if (user?.role === 'head' || hasFullAccess) navItems.push('/entities/review')` للمراجعة.

### 7-ط) سجل التغييرات — Payload ناقص
- **الوصف الأصلي المتكرر:** «يسجل هذا التغيير بكل تفاصيله مع المدير أو المشرف الذي قام به في سجل تغييرات الجهة العامة».
- **التعارض:** `ActionKindCatalog.cs:11` يملك `Abolish` لكن لا كاتب له بعد، و`EntityChangeLog.tsx:15`
  يعرض `ACTION_LABELS` لكل الأنواع. الخطة تذكر `ActionKind=Abolish` للحلول لكنها لم تحدد محتوى `PayloadJson`.
- **التسريب إن لم يُسد:** سجل بلا `oldCanonicalNames[]` و`newCanonical` و`decreeKind/Number/Date` وخريطة الفروع
  لن يلبّي "بكل تفاصيله" ولن يظهر في تصدير `ExportChangeEventsAsync:737`.
- **الإلزام قبل الاعتماد:** كل `PublicEntityChangeEvent` للأفعال الثلاثة يتضمن `oldCanonicalNames[]` و`newCanonical`
  و`decreeKind/Number/Date` (عبر `FreeDateParser.Parse` — قاعدة التواريخ الحرة `AGENTS.md`) وخريطة الفروع
  وأثر الملفات، كما في `CommitMergeAsync:1701` (`branchMap`).

---

## 8) تحسينات مقترحة تزيد الحرفية (لا تُعدّل قبل موافقتك)

> هذه تحسينات مقترحة بعد سد الثغرات أعلاه — **البنود 1 و2 و5 أصبحت إلزامية** بعد مراجعة الخبير
> (معاينة، حفظ الأسماء البديلة، توحيد نص المرجع) والبقية تُعتمد بندًا بندًا.

1. **معاينة قبل التنفيذ** على غرار `merge-preview`/`unify-preview`: أضف `POST /groups/rename-preview` و
   `POST /groups/abolish-preview` تعيد عدد الملفات المتأثرة وقائمة الفروع، قبل الزر الأحمر "تأكيد".
   يمنع تنفيذًا خاطئًا لا رجعة فيه (النصوص تُعاد كتابتها في `SyncTextsAfterRenameAsync:941`).
   **[إلزامي]** — الفعل لا رجعة فيه على النصوص.

2. **حفظ الاسم القديم كاسم بديل**: بعد `RenameGroupAsync` أضف `oldCanonical` إلى `PublicEntityAlias` للجهة الأم
   (كما يفعل الدمج `PublicEntityService.cs:1653`) ليبقى البحث بالاسم القديم يعثر على الجهة — يحل مشكلة
   الملفات التنفيذية القديمة المكتوبة بالاسم القديم.
   **[إلزامي — حجّة قانونية]** — حفظ الاسم القديم كـ `Alias` (وليس مجرد إعادة كتابة النصوص) هو ما يضمن
   الرجوع للملفات التنفيذية القديمة المختومة بالاسم السابق؛ الدمج الحالي يفعلها لكن `RenameGroupAsync`
   المقترح في أ2 لم تكن تنص عليها.

3. **تأكيد بكتابة الاسم**: كما في `MergeModal` (تأكيد بكتابة اسم الناجي)، اجعل `تعديل تسمية/دمج/حلول`
   يتطلب كتابة الاسم الجديد/الهدف لتأكيد — يقلل خطر النقر الخاطئ على زر أحمر.

4. **(مُدمج في 7-ج)** — فلترة بحث الهويات الأم (إضافة `excludeIds` ومنع الاختيار المتبادل) أُدرجت ضمن
   الإصلاح الإلزامي في 7-ج لتجنّب الازدواج. لا تُعامل كبند مستقل.

5. **توحيد مصدر نص التنبيه/الوقعة**: استخرج دالة `BuildDecreeSuffix(decreeKind, decreeNumber, decreeDate)`
   واحدة تُستخدم في `HeadAlert.Message` و`DocumentOccurrence.Details` و`PublicEntityChangeEvent.PayloadJson`
   لضمان تطابق النصوص الثلاثة حرفيًا مع طلبك («بموجب كذا "القرار أو القانون أو المرسوم"»).
   **[إلزامي — حجّة إثباتية]** — أي تفاوت بين التنبيه والوقعة وسجل التغييرات يُعد عيبًا إثباتيًا قانونيًا.

6. **إعادة توجيه الروابط القديمة**: احتفظ بـ `Route /entities/registry → Navigate to /entities/review-management?tab=add`
   و`/entity-change-log → ?tab=log` لمدة إصدار واحد مع بانر تنبيه، بدل حذف فوري يكسر الإشارات المرجعية.

7. **اختبار عزل البوابة**: أضف اختبارًا يؤكد أن إعادة تسمية/دمج/حلول لا تُظهر سجلًا `NeedsReview` للمندوب قبل
   الاعتماد (المواءمة السلوكية `docs/ENTITY_PORTAL_PLAN.md §6bis`).

8. **حد أداء البث**: عند أكثر من 50 فرعًا، ادفع التنبيهات دفعات `SaveChangesAsync` كل 20 فرعًا لتجنب معاملة
   ضخمة واحدة تحبس `HeadAlerts` — كما في `CommitMergeAsync:1757` (`await _uow.SaveChangesAsync(token)` مرة واحدة).

9. **توثيق نصوص التنبيهات**: وحّد صيغة التنبيه العام «يرجى ملاحظة أنه تم تعديل اسم "كذا" الى "كذا" بموجب كذا»
   و«يرجى ملاحظة أنه تم دمج "كذا" مع "كذا" بموجب كذا» و«يرجى ملاحظة أنه تم حلول "كذا" محل "كذا" بموجب كذا»
   في ملف ثابت واحد (مثل `EntityChangeMessages.cs`) بدل تكرارها في كل خدمة.

10. **تحديث الوثائق**: بعد التنفيذ، حدّث `docs/ENTITY_MERGE_MOVE_SPEC.md` و`docs/ENTITY_PORTAL_PLAN.md`
    بمسارات `rename/abolish-and-replace` الجديدة، وإلا ستبقى الوثائق تشير لمسارات قديمة.

