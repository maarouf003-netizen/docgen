# سجل التغييرات (Changelog)

نظام **«مسار»** — مساعد محامي الدولة الذكي في إدارة الملفات التنفيذية.

تُوثَّق كل التغييرات الجوهرية في هذا الملف وفق معيار
[Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)
وبترقيم دلالي [SemVer](https://semver.org/lang/ar/) بدأ استخدامه بأثر رجعي من `1.0.0`.

- **Added**: ميزات جديدة · **Changed**: تعديل سلوك قائم · **Fixed**: إصلاح عطل · **Security**: معالجة أمنية
- التواريخ بصيغة `YYYY-MM-DD`.
- أي إصدار يتضمن هجرات قاعدة بيانات يُحمل قسم **«الهجرات الإلزامية عند النشر»** — تطبيقها شرط لعمل النظام
  (المرجع العملي: `RUN_GUIDE.md` §9).

---

## [1.11.0] — 2026-08-28

### Changed — نافذة اختيار الجهة العامة + أدوات إدارة السجل + تغطية الإنابة
- **نافذة «اختيار جهة عامة» (`PublicEntityPickerModal`)**: أصبحت قائمة منسدلة تُفلتر برقم الفرع
  (`branchName`) — «الفرع الرئيسي» يثبَّت أعلى القائمة، وتوسّع `GET /api/entity-registry/search`
  و`/list` بمعامل `branchName` عبر `PublicEntityService.ListAsync` للفلترة من الخادم للجهات
  المتوافقة مع فرع المُدخِل.
- **أدوات إدارة سجل الجهات (مرحلة 5)**: مودالات اقتراح التعديل `ProposeEditModal`، توحيد الأسماء
  `UnifyNamesModal`، الدمج `MergeModal`، وإدارة الفروع `BranchManagementModal` — مع صفحة سجل
  التغييرات `EntityChangeLog` واختباراتها.
- **تغطية الإنابة `SaleCoversFullDebt`**: حقل يحدد إن كان بيع المندوب يُغطي كامل الدين، مع هجرته
  للسياقين — يعمل به في إكمال الإنابة `CompleteDelegationModal` وتفاصيلها `DelegationDetails`.
- **بنية معمارية**: ملفان `ARCHITECTURE_MAP.md` و`docs/ARCHITECTURE_MAP.md` يوثّقان بنية النظام.

### الهجرات الإلزامية عند النشر
| السياق | الهجرة |
|---|---|
| `DocGeneratorDbContext` (SQLite) | `AddDelegationSaleCoversFullDebt` |
| `DocGeneratorPostgresDbContext` (PostgreSQL) | `AddDelegationSaleCoversFullDebt` |

> ⚠️ يجب تنفيذها في نافذة النشر قبل تشغيل الإصدار:
> `dotnet ef database update --context DocGeneratorDbContext` ثم
> `dotnet ef database update --context DocGeneratorPostgresDbContext` —
> وإلا فشل إكمال الإنابة (`no such column`). التفاصيل: `RUN_GUIDE.md` §9.

## [1.10.0] — 2026-08-26

### Changed — بوابة الجهات: نموذج حوكمة جديد «الدخول الفوري والمراجعة اللاحقة»
قرار صاحب المشروع بإلغاء مسار «اقتراح بانتظار الاعتماد» واستبداله بـ:
- **ما يُدخله المحامي يُعتمد أصوليًا نهائيًا فورًا** ويصبح قابلًا للربط وللظهور في البوابة
  والإحصاءات لحظيًا، لكنه يبقى `NeedsReview` مع تنبيه لرؤساء الأقسام النشطين في محافظته:
  «المحامي {فلان} أدخل جهة عامة جديدة … يرجى مراجعتها».
- صفحة جديدة **«مراجعة سجل الجهات العامة الممثلة»** لرئيس القسم (محافظته حصرًا) وللمدير/المشرف
  (كل السجل): قائمة الباحث مراجعة باسم المُدخِل، مع **اعتماد كما هو** (يقفل المراجعة بصمت)،
  و**تعديل** البيانات/التسمية — وتغيير التسمية أثناء المراجعة يوجّه تنبيهًا للمحامي المُدخِل:
  «تم تعديل اسم الجهة التي أدخلتها من “القديم” إلى “الجديد”» — ويقفل المراجعة تلقائيًا.
- الصفحة نفسها تتيح **إدخال جهات مسبقة بفروعها** (الرئيس ضمن محافظته، والإدارة لأي محافظة).
- إعادة تسمية من الإدارة تبقى فورية التنفيذ على كل الملفات المرتبطة في كل الفروع (د5) كما كانت.

### Removed
- مسار الاقتراحات القديم بالكامل: نقاط النهاية `proposals*`، وصفحة «اقتراحات الجهات»، وبطاقة
  لوحة التحكم المرتبطة، وجدول `PublicEntityProposals` (استُبدل بنموذج المراجعة أعلاه).

### Added
- `GET /api/entity-registry/pending-review` و`POST /api/entity-registry/{id}/approve-review`.
- بحث نافذة الإدخال يستبعد القيود الموقوفة (`IsActive=false`) فلا يمكن ربطها أو ربط مندوب بها.

### الهجرات عند النشر
| السياق | الهجرة |
|---|---|
| `DocGeneratorDbContext` (SQLite) | `AddEntityRegistryReview` |
| `DocGeneratorPostgresDbContext` (PostgreSQL) | `AddEntityRegistryReviewPg` |

> ⚠️ **تحذير فقدان بيانات**: هذه الهجرة تُسقط جدول `PublicEntityProposals` نهائيًا (نموذج ملغى)
> وتضيف أعمدة المراجعة إلى `PublicEntities`. نفّذها في نافذة النشر للسياقين — وإلا فشل تشغيل
> السجل (`no such column`). تفاصيل §9.

## [1.9.0] — 2026-08-24

### Added — بوابة الجهات العامة: المرحلة 4 (إحصاءات الجهة)
- نقطة `GET /api/portal/stats` قرائية داخل بوابة المندوب تعيد:
  - عدادات الحالة الخمسة (الإجمالي/متداول/منفذ/تريث/تحت رفع) **مطابقةً حرفيًا لفلاتر
    القائمة**، مع استبعاد المشطوب دائمًا كما في القائمة.
  - سلسلة آخر 12 شهرًا متصلة حتى الشهر الحالي شاملة الأشهر الصفرية.
  - توزيع الارتباط على قيود النطاق (بقيدٍ نهائي نشط)؛ الملف المرتبط بأكثر من قيد
    يُحتسب تحت كل قيد ارتبط به (توزيع ارتباط موثّق لا تجزئة حصرية)، وتظهر القيود
    الصفرية أيضًا ليعرف المندوب أين لا يوجد نشاط.
  - أعلى العملات بعدد الملفات ومجموع مبالغها **داخل العملة نفسها فقط** — بلا خلط
    عملات إطلاقًا، والعملة الفارغة تُعرض «غير محددة».
  - الاستئنافات: معلّقة مقابل مغلقة ضمن النطاق حصرًا.
- كل الحسابات تجري فوق `ScopePredicate` الموحد (مصدر حقيقة واحد للرؤية) فلا يمكن
  أن يعطي الإحصاء رقمًا يخالف نتيجة القائمة أو التفاصيل.
- الواجهة: بطاقة «إحصاءات نطاق جهتك» أعلى صفحة ملفات الجهة — عدادات، رسم أعمدة
  شهري (12 دلوًا بأعمدة RTL وأسماء أشهر عربية)، أشرطة توزيع القيود، وجدول العملات.

### Changed
- لا شيء كسِر؛ إضافة قرائية فقط فوق الأعمدة والفهارس القائمة.

### الهجرات عند النشر
> ✅ **لا هجرات جديدة في هذا الإصدار** — الإحصاءات قرائية فوق بنية المرحلتين 2–3.
> تنبيهات الهجرات السابقة (`AddEntityRegistry` ← `…Links` ← `…Portal`) باقية كما هي في §9.

## [1.8.0] — 2026-08-24

### Added — بوابة الجهات العامة: المرحلة 3 (بوابة المندوب)
- **حسابات المندوبين داخل النظام** (د11): دور `entitymanager` يُضافه المدير/المشرف/رئيس القسم من
  شاشة «مندوبو الجهات»، وكل حساب **مربوط بنطاقه** — هوية أم بكل قيودها أو قيدًا بعينه
  (أحدهما حصرًا، بتحقق خدمي ورسائل عربية)، مع تعديل الاسم/التفعيل/إعادة كلمة المرور
  (تبطل الجلسات برفع `TokenVersion`) وإعادة ربط النطاق لاحقًا.
- **مسارات بوابة قرائية حصرية** `api/portal/*` (د10): `my-scope` و`files` (بحث/فلتر حالة/
  ترقيم) و`files/{id}` التفاصيل الكاملة و`files/{id}/appeals` بطاقة الاستئنافات القرائية
  و`export` تصدير Excel بنفس الفلاتر وبسقف `ExportOptions.MaxRows`.
- **قاعدة الرؤية «أي تطابق طرفي»** (د1): يرى المندوب كل ملف فيه طرف مرتبط بقيد من نطاقه،
  و**قيود الانتظار لا تظهر إطلاقًا حتى اعتمادها** (د4) — الفلترة تتحقق من حالة القيد عبر
  الانضمام لا من نسخة التسريع وحدها.
- **عزل بنيوي شامل**: وسيط `EntityManagerPortalGuard` يمنع دور المندوب من **كل** مسارات API
  عدا بوابته و«من أنا/تسجيل الخروج» بـ403 فورًا؛ ومسارات البوابة نفسها GET فقط لا نقاط كتابة
  فيها، والخروج عن النطاق يعود **404 دون كشف وجود الملف**.
- **خصوصية التصدير**: أعمدة المحامين الداخلية (فرع الإدارة/المحامي المختص/العدادات) مخفية
  دائمًا عن مصنّف البوابة.
- **سجل تدقيق جديد**: `view_entity_portal_files` عند دخول جلسة عرض ملف، و
  `export_entity_portal_excel` عند كل تصدير بعدد الصفوف.
- الواجهة: صفحة «ملفات الجهة» (شريط النطاق + بحث + فلتر حالة + ترقيم + زر تصدير)، صفحة
  تفاصيل قرائية ببطاقات أساسية/أطراف/استئنافات، شاشة «مندوبو الجهات» للإدارة، وقائمة تنقل
  المندوب مختصرة إلى بوابته حصرًا مع تحويل لوحة التحكم إليها تلقائيًا.

### الهجرات الإلزامية عند النشر
| السياق | الهجرة |
|---|---|
| `DocGeneratorDbContext` (SQLite) | `AddEntityPortal` |
| `DocGeneratorPostgresDbContext` (PostgreSQL) | `AddEntityPortalPg` |

> ⚠️ يجب تنفيذها في نافذة النشر قبل تشغيل الإصدار:
> `dotnet ef database update --context DocGeneratorDbContext` ثم
> `dotnet ef database update --context DocGeneratorPostgresDbContext` —
> وإلا فشل تسجيل الدخول/الإدارة (`no such column`). التفاصيل: `RUN_GUIDE.md` §9.

## [1.7.0] — 2026-08-24

### Added — بوابة الجهات العامة: المرحلة 2 (نافذة الإدخال وربط الملفات)
- **ربط الملفات بالسجل المرجعي**: عمود `RegistryId` (FK بـSetNull + فهرس) على صفَّي
  `ApplicantPublicEntities` و`ExecutedPublicEntities`، وعمود تسريع `ApplicantRegistryId`
  على `Documents` يُحدَّث آليًا عند كل حفظ من أول ربط غير فارغ بين صفوف الطالب، ويُصفَّر
  تلقائيًا عند تفريغ القائمة أو الانتقال لوضع «منفذ عليه».
- **ظهور الربط في سجل تعديلات الحقول تلقائيًا**: محرك التتبع يلتقط الحقل الجديد فورًا
  بتسمية عربية «ربط جهة الطالب بالسجل المرجعي» وقيم قبل/بعد.
- **نافذة «اختيار الجهة العامة»** (`PublicEntityPickerModal`) في نموذجَي الطالب والمنفذ:
  بحث واحد بنتائج قابلة للتبديل حسب المحافظة، اقتراحات الفرع مستخلصة من القيود المطابقة،
  شارة تمييز «بانتظار الاعتماد» لقيود الانتظار التي يجوز للمحامي ربطها على ملفه،
  و**بلا أي عدّاد ملفات** (د9). زر «اختيار من السجل…» بجوار كل جهة، وشارة
  «مرتبطة بالسجل ✓» مع **فكّ تلقائي للربط عند أي تحرير يدوي لنص الجهة** حفاظًا على الاتساق.
- **نموذج اقتراح جهة جديدة داخل النافذة** (د7/د8) بترتيب الحقول المعتمد: نص التحذير
  العنابي الحرفي فوق اسم الجهة، والـplaceholder المعتمد، ثم قائمة الصيغة
  («إضافة لوظيفته/إضافة لمنصبه») فالنوع فالمحافظة فالفرع — والحفظ يرسل الاقتراح
  بحالة انتظار مع معرّف الملف المصدر ويعرض رسالة نجاح توضح انتظار الاعتماد.
- ربط الإنابات: نسخ صفوف جهات الطالب إلى الملف المناب تشمل `RegistryId` مع تحديث نسخة التسريع.

### Changed
- بحث السجل `GET /api/entity-registry/search` يعيد الآن قيود الانتظار لكل الطاقم عدا دور
  المندوب؛ تظل مخفاية عن البوابة نفسها حتى اعتمادها (§5.3/د4).

### الهجرات الإلزامية عند النشر
| السياق | الهجرة |
|---|---|
| `DocGeneratorDbContext` (SQLite) | `AddEntityRegistryLinks` |
| `DocGeneratorPostgresDbContext` (PostgreSQL) | `AddEntityRegistryLinksPg` |

> ⚠️ يجب تنفيذها في نافذة النشر قبل تشغيل الإصدار:
> `dotnet ef database update --context DocGeneratorDbContext` ثم
> `dotnet ef database update --context DocGeneratorPostgresDbContext` —
> وإلا فشل الحفظ/التشغيل (`no such column`). التفاصيل: `RUN_GUIDE.md` §9.

## [1.6.0] — 2026-08-24

### Added — بوابة الجهات العامة: المرحلة 1 (السجل والحوكمة والاستيراد)
- **سجل مرجعي مركزي للجهات العامة** بمستويين (د2): هوية أم `PublicEntityGroup` بالاسم المعتمد
  الفريد + قيود `PublicEntity` (المحافظة + الفرع) مع أسماء بديلة `PublicEntityAlias` للبحث،
  وحالة كل قيد `final/pending` (د4).
- **اقتراحات المحامين** (`PublicEntityProposal`): تقديم اقتراح جهة جديدة بحالة انتظار لا يظهر
  لأي بوابٍ قبل الاعتماد، واعتماده من رئيس القسم ضمن محافظته فينشئ القيد النهائي مباشرة،
  أو رفضه بسبب إلزامي يُعرض لمقدم الاقتراح.
- **إعادة التسمية الجماعية الفورية** (د5): تغيير الاسم المعتمد يزامن الأعمدة النصية في كل
  الصفوف المرتبطة (`ApplicantPublicEntities.Name` و`Document.Applicant` المشتق منها
  و`ExecutedPublicEntities.EntityName`) ويحدّث نص البحث، ضمن معاملة واحدة مع تدقيق حقول
  قبل/بعد لكل ملف متأثر عبر محرك `DocumentChangeTracker`.
- حدود رئيس القسم مفروضة خدميًا ومتحكميًا: تعديل قيود محافظته حصرًا، وإعادة تسمية هوية
  تمتد لمحافظات أخرى = `403` (د5/د6).
- **أداة الاستيراد التاريخي الإدارية** (د12): `POST /api/entity-registry/import-preview`
  يجمع الكتابات المتمايزة (طالب/منفذ) بعد تطبيع `ArabicNameNormalizer` مع عدّادات ملفاتها
  ويقترح الأكثر تكرارًا، ثم `import-commit` يعتمد الربط الجماعي بقيود **نهائية مباشرة** مع
  تسجيل الكتابات المخالفة للتطبيع أسماءً بديلة، ويتجاهل البند المعاد اعتماده بهدوء.
- شاشة **«سجل الجهات العامة»** لرئيس القسم/المدير/المشرف: فلاتر المحافظة والحالة والبحث
  المتسامح عربيًا، إضافة وتعديل القيود والأسماء البديلة، وأداة الاستيراد للمدير/المشرف؛
  وصفحة **«اقتراحات الجهات»** وبطاقة عدّادية في لوحة رئيس القسم.
- تحديث **إدارة الفروع** بحقل «المحافظة» الذي يضبط نطاق رئيس القسم في السجل.
- كتالوجات جديدة: `PublicEntityTypeCatalog` / `EntityStatusCatalog` / `CitationFormulaCatalog`
  (صيغتا «إضافة لوظيفته/لمنصبه» — د8) وحالة اقتراح `ProposalStatus`، ودور `EntityManager`
  في `UserRole` تمهيدًا للمرحلة 3.

### الهجرات الإلزامية عند النشر
| السياق | الهجرة |
|---|---|
| `DocGeneratorDbContext` (SQLite) | `AddEntityRegistry` |
| `DocGeneratorPostgresDbContext` (PostgreSQL) | `AddEntityRegistryPg` |

> ⚠️ يجب تنفيذها في نافذة النشر قبل تشغيل الإصدار:
> `dotnet ef database update --context DocGeneratorDbContext` ثم
> `dotnet ef database update --context DocGeneratorPostgresDbContext` —
> وإلا فشل التشغيل (`no such table/column`). التفاصيل: `RUN_GUIDE.md` §9.

## [1.5.0] — 2026-08-24

### Added — نظام كتب المطالعة (الميزة الكاملة)
- **كتاب مطالعة** يسطّره المحامي لرئيس قسمه: عامًا غير مرتبط بملف، أو مربوطًا بملف تنفيذي حصرًا
  (زر «تسطير كتاب مطالعة» في تفاصيل كل ملف).
- توليد آلي للرقم بصيغة `{رمز الفرع}-{السنة}-{عشوائي 4 خانات}` مع ضمان التفرّد، وتاريخ الكتاب من لحظة
  الإرسال؛ والكتاب المرسل وثيقة رسمية غير قابلة للتعديل أو الحذف.
- **لاحق (addendum)** يضيفه محامي الكتاب برقم وتاريخ مستقلّين، ويعيد الكتاب إلى «بانتظار رد».
- **رد رئيس القسم** برقم وتاريخ مولَّدين تلقائيًا، مقصورًا على كتب فرعه؛ والمدير والمشرف اطّلاع قراءة فقط.
- صفحة **«المطالعات»** للمحامي وصفحة **«كتب المطالعات»** لرئيس القسم (بحث نصي في الرقم واسم المنفذ عليه
  ونصوص الرسائل، ترقيم صفحات، شارة خضراء «تم الرد» / حمراء «بانتظار رد»)، وصيغة السطر المعتمدة:
  «مطالعة بملف (الاسم الثلاثي) رقم.. نوع.. لعام.. دائرة تنفيذ..» أو «كتاب مطالعة عام غير مرتبط بملف».
- بطاقة «كتب المطالعة» في تفاصيل كل ملف تظهر كتابَه حصرًا لمالكه ورئيس قسمه والإدارة ولكل محامٍ
  يتابعه بإحالة أو إنابة أو استئناف.
- **تنبيه الرد للمحامي**: تنبيه تلقائي في لوحة التحكم لحظة الرد (مع دمج الردود المتتالية غير المقروءة
  في تنبيه واحد)، ورابط مباشر من التنبيه إلى الكتاب، وشارة «رد جديد» على سطر الكتاب وعدّاد أحمر على
  بند المطالعات حتى الفتح — وحالة الإطلاع تُحجب عن غير صاحب الكتاب حفاظًا على خصوصيته.
- **سجل تعديلات الملف على مستوى الحقول**: كل تعديل (نموذج الملف وتغيّرات الحالة كافة) يوثّق الحقول
  المتغيرة قيمةً قبل/بعد بتسميات عربية مجمّدة، ويشمل توقيعات المجموعات (كفالات/ورثة/أصول…)؛ يُعرض
  عبر بطاقة «سجل التعديلات» في تفاصيل الملف بصلاحية المالك ورئيس القسم والإدارة.
- تسجيل كامل في سجل التدقيق: `create_review_letter` / `add_review_letter_addendum` / `reply_review_letter`.

### Changed
- توسعة المحرر الغني بخيارات **نوع الخط وحجمه** (إضافة إلى الألوان والعريض) في كل مواضعه، مع مزامنة
  قائمة التعقيم البيضاء بين الواجهة والخلفية (`font-family` / `font-size`) وقيم آمنة فقط.

### Fixed
- هجرة PostgreSQL تاريخية (`AddExecutedExecutionDates`) كانت تفشل عند التثبيت من قاعدة فارغة بسبب نوع
  `datetime2` غير الصالح هناك — صحّحت إلى `timestamp with time zone` (تعديل آمن: لا يمكن أن تكون طُبّقت
  بنجاح قبل التصحيح).
- انحراف كان يحوّل رابط تنبيه الاستئناف في بطاقة التنبيهات إلى الملف بدل تفاصيل الاستئناف (أمسكه اختبار
  التكامل القائم).
- جلب مزدوج لقائمة المطالعات بعد التسطير عندما تكون الصفحة الحالية > 1.

### Security
- امتداد قائمة خصائص CSS المسموحة في تعقيم HTML (خلفيًا عبر Ganss.Xss وأماميًا عبر DOMPurify) بقيم
  مقيّدة بنمط آمن يمنع `url()` و`expression`.

### الهجرات الإلزامية عند النشر
| السياق | الهجرات |
|---|---|
| SQLite (`DocGeneratorDbContext`) | `AddReviewLetters` → `AddReviewLetterNotifications` → `AddDocumentFieldChanges` |
| PostgreSQL (`DocGeneratorPostgresDbContext`) | `AddReviewLettersPg` → `AddReviewLetterNotificationsPg` → `AddDocumentFieldChangesPg` |

```powershell
dotnet ef database update --context DocGeneratorDbContext
dotnet ef database update --context DocGeneratorPostgresDbContext
```

> ملاحظة ترقية: إن سبق تطبيق `AddReviewLettersPg` باسم `20260824060842_AddReviewLettersPg` على بيئة
> تجريبية قبل تصحيحه، احذف سطرها من `__EFMigrationsHistory` ثم طبّق البديل `20260824064804_…`.

---

## [1.4.0] — 2026-08-23

### Added — الاستئنافات
- تسطير استئناف على الملف التنفيذي باتجاهيه «مستأنِفين / مستأنف علينا» مع لقطات الأطراف وقت الإنشاء.
- دورة حياة كاملة: منظور ← محسوم (قرار ومنطوق ونتيجة للصالح/للضد) أو مشطوب، مع أرقام أساس استئنافية
  تدور سنويًا وسجل أرقام تاريخية.
- إسناد الاستئنافات من رئيس القسم لمحامي الفرع ونقلها جملةً، ومتابعتها من المحامي المسند إليه
  (رؤية قراءة لتفاصيل الملف)، وإجراءات وملاحظات خاصة بالاستئناف مع تذكيرات ملوّنة.
- صفحة قائمة استئنافات وبحث، وبطاقة استئنافات داخل وقوعات الملف، وإحصاءات استئنافات في لوحات الإدارة.

### الهجرات الإلزامية عند النشر
- SQLite: `AddDocumentAppeals` · PostgreSQL: `AddDocumentAppealsPg`

---

## [1.3.0] — 2026-08-17 → 2026-08-22

### Added — الإنابات والأصول والتحسينات المؤسسية
- **الإنابات التنفيذية**: تسطير إنابة لدائرة أخرى، إنشاء الملف المناب، اعتماد رئيس القسم واختيار
  المحامي المختص، تسجيل الأصول وإتمام الإنابة («منفذ إنابة»)، نافذة «طلبات الإنابة»، وأصول الإنابة
  (عقار/مركبة/متجر) بلقطتها، وتنبيهات مرحلية آلية.
- أنواع الأصول الموحدة (عقار/مركبة/متجر مسجل وغير مسجل/كفالة رواتب) بملاك متعددين وأنصبة وتاريخ
  إلقاء الحجز، مع عرض موحد للأصول.
- تحويل «بدل المبيع للجهة العامة» بتاريخه ولقطة معدّلة، وعمود «فرع الإدارة» للمدير/المشرف.
- ربط تنبيهات رئيس القسم بالإنابة لتصفيتها الآلية (`HeadAlertDelegationLink`).
- ضبط أنواع التواريخ في PostgreSQL (`AlignPostgresTimestampTypes`).

### Fixed
- إزالة أعمدة كتب الإرسال المهجورة (`RemoveSendBookColumns`).

### الهجرات الإلزامية عند النشر
- SQLite: `RenameAssetsAndAddKinds`, `AddDelegations`, `AddHeadAlertDelegationLink`,
  `RemoveSendBookColumns`, `AddForcibleTransferAndSnapshotAdjusted`, `AlignPostgresTimestampTypes`,
  `AddAssetSeizureDate`
- PostgreSQL: نظائرها المنتهية بـ `Pg` (بما فيها `AddPostgresPendingSchema`)

---

## [1.2.0] — 2026-08-09 → 2026-08-15

### Added — الأطراف والحالات والعملات المتعددة
- جهات التنفيذ بأنواعها (طبيعي/كيان عام/ورثة) بممثليهم وقدراتهم، وطبيعة الطرف، ومحافظة الكيان العام.
- إسناد الملفات (سجل تعاقب المحامين) ونقل الملف بين المحامين برئيس القسم.
- تجديد الملف المشطوب (اخطار التجديد وتاريخه ورقمه)، والوقوعات (occurrences) بأنواعها.
- العملات الثلاث في كل مبالغ الملف (المطلوب/المقبوض/المدفوع/التضمين) وتاريخا التنفيذ والإيداع،
  و«كان منفذًا واودع», وحالة «منفذ بالتسوية» وحقول تغيير الحالة.
- إحالة الملف من محامٍ آخر (`ReferredFromLawyer/ReferredAt`)، رقم ورود الملف وتاريخه، اسم الوريث الثلاثي،
  تاريخ تسجيل المتجر محلولًا (`RegistrationDateParsed`).
- كتاب الملحق (نوعه ورقمه وتاريخه) للعقود.

### Fixed
- فهرسة فريدة جزئية لأسماء المستخدمين بلا فرع.

### الهجرات الإلزامية عند النشر
- SQLite: من `AddExecutedRequiredCurrencies` حتى `AddContractAnnex` (12 هجرة)
- PostgreSQL: نظائرها + `AddPostgresPendingSchema`

---

## [1.1.0] — 2026-08-01 → 2026-08-08

### Added
- «الإجراءات والملاحظات» على الملف (action/note) بمحرر غني معقّم، مع تذكيرات مبرمجة بمدة ولون
  وتذكيرات الاستئناف لاحقًا.
- **تنبيهات رئيس القسم** (`HeadAlerts`): بملف أو رسالة خاصة أو تعليم فرعي، بمستلمين وعلامات قراءة
  وعدّاد غير مقروء.
- **تدوير أرقام الأساس** السنوي للأرقام التنفيذية (`DocumentBaseNumbers`).
- الحذف المنطقي للملفات (سلة المحذوفات والاستعادة) ومالك الملف الإلزامي.
- تدوير أرقام الأساس، الورثة للطرفين، ملاك العقارات، تاريخ التسجيل المفسر زمنيًا.
- بدء دعم PostgreSQL بهجرات منفصلة (`InitialCreate` بتاريخ 2026-08-08).

### Security
- إبطال الجلسات عند تغيير كلمة المرور (`TokenVersion`)، وقفل الحساب بعد المحاولات الفاشلة
  (`AccountLockout`) مع محدد المعدل.

### Fixed
- رفع حدود طول نص الإجراء (`MakeExecutionActionTextUnlimited`)، وتفرد اسم المستخدم ضمن الفرع.

### الهجرات الإلزامية عند النشر
- SQLite: من `AddExecutionActions` حتى `AddRealEstateOwners` · PostgreSQL: `InitialCreate` فما بعدها

---

## [1.0.0] — 2026-07-31

### Added — النواة التشغيلية الأولى
- إدارة الملفات التنفيذية كاملة: طرفا المقترض والجهة العامة بكفالاتهم وورثتهم وأصولهم، السند التنفيذي،
  الحالات التنفيذية، توليد المستندات Word، تصدير Excel.
- المصادقة بجلسة Cookie (HttpOnly, SameSite=Strict) + حماية CSRF على كل الكتابات، بأربعة أدوار:
  محامٍ / رئيس قسم / مدير / مشرف نظام، ومصفوفة صلاحيات مركزية `RolePermissions`.
- الفروع والمستخدمون، سجل التدقيق، محاولات الدخول، رقم تحت الإيداع.
- واجهة React + TypeScript + Tailwind (RTL) بمخطط مكتب/جوال (درج وتنقل سفلي) واختبارات Vitest.

### الهجرات الإلزامية عند النشر
- SQLite: `InitialCreate`, `AddLoginAttempts`, `AddUnderFilingNumber`

---

> ملاحظة أرشيفية: الترقيم الدلالي بدأ استخدامه بأثر رجعي عند اعتماد هذا السجل في 2026-08-24؛
> الإصدارات الأقدم من `1.5.0` مجمّعة حسب دفعات الميزات الفعلية كما وردت في هجرات قاعدة البيانات
> وسجل التدقيق، دون وسوم Git سابقة لها.

