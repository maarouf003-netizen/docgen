# سجل التغييرات (CHANGELOG)

## [غير مُصدَر] — 2026-09-24
### ميزة المراسلات (تواصل مندوب↔محامٍ/رئيس قسم)
- كيان مستقل `Correspondences` + `CorrespondenceMessages` + `CorrespondenceReceipts` (هجرتان: `AddCorrespondences`/`AddCorrespondencesPg` — تُطبَّق عند النشر على السياقين).
- رقم تلقائي `{الرمز}-{السنة}-{عشوائي}` (من فرع الملف، أو محافظة الجهة للعامة من مندوب) + تاريخ تلقائي + أهمية ثابتة (`normal/important/urgent`) + توثيق مشاهدة (من؟ متى؟) بقيد فريد.
- الكتابة للأطراف فقط (اللاحق للمنشئ، الرد للمستلم المعيَّن)؛ كتابة المندوب حصرًا عبر مسارات البوابة دون مساس بحارس العزل.
- الرؤية: الطرفان دائمًا، الرئيس لمحافظته (فرعه + العامة من مندوبيها)، المدير/المشرف بمحافظة منتقاة إجباريًا؛ جرس العاجل للمستلم فقط.
- الواجهة: قائمة + تفاصيل + بطاقة ملف + زر `مراسلات` جانب التعديل + بوابة مندوب (`مراسلات الجهة`) + شارات.
- إصلاحات مراجعة نقدية (لا هجرات جديدة): تقييد مساري `/correspondence` الرئيسيين بأدوار الكتابة/الإدارة (المندوب للبوابة حصرًا) + زر الإنشاء واعٍ بالمسار؛ عقد أنواع صارم (`T | null` للحقول المرسلة دائمًا)؛ استطلاع واحد لعدّاد العاجل يغذّي الشارة والأجراس؛ تسمية دور المندوب؛ مؤشر «تمت المشاهدة» في بطاقة الملف؛ سقف المرشحين `TargetsLimit` مصدره الوحيد ويُطبَّق في المستودع.
### أهلية المرشحين بحسب ربط المراسلة (لا هجرات جديدة)
- المراسلة المربوطة بملف تُقيَّد الأهلية تلقائيًا في البحث والإنشاء (مصدر واحد في الخدمة): مندوب الجهة لا يستهدف إلا «محامي الملف» (المالك + المسند بالاستئناف/الإنابة)، والمحامي/الرئيس لا يستهدف إلا مناديب نطاق الملف (قيد الملف النهائي + مجموعته الأم)، دون رؤساء أو زملاء أو مناديب خارج النطاق.
- المراسلة العامة تبقى مفتوحة كما كانت (البحث حر مثلًا: الرؤساء مدرجون).
- مسارا البحث `GET /correspondence/targets` و`GET /portal/correspondence/targets` يقبلان `documentId` اختياريًا ويعيدان 400 عند مستلم غير مؤهل في الإنشاء (و403 عند خارج صلاحية الملف).
- الواجهة: نافذة الإنشاء تمرّر `documentId` مع البحث وتظهر رسالة «لا يوجد مستلم مؤهل…» للملف؛ إعادة تسمية فلتر «كل الأهميات» → «أهمية المراسلة».
- اختبارات: مصفوفة الأهلية الخلفية (بحث/إنشاء/استبعادات/رفض ثنائي الاتجاه) + 4 سيناريوهات تكامل API + اختبارات نافذة الإنشاء.
### تقوية سلامة الترقيم في المراسلات (لا هجرات جديدة — بلا أي تغيير مخطط)
- إنشاء المراسلة يعيد توليد الرقم تلقائيًا عند اصطدام القيد الفريد (سباق تسطيرين متزامنين) بدل خطأ 500 — حتى حد المحاولات ثم رسالة «تعذر توليد رقم فريد…» المفهومة؛ التدقيق يُسجَّل مرة واحدة داخل المحاولة الناجحة فقط.
- ترقيم اللاحقات/الردود أصبح حتميًا داخل نطاق المراسلة (`{رقم المراسلة}-{التسلسل}`؛ الأولى برقم المراسلة نفسه) بدل التخمين العشوائي العام — بلا سباق ولا غموض ترقيم؛ الصفوف القديمة كما هي.
- اختبارات: تعارض حقيقي من المزود (إعادة محاولة ناجحة + نفاد المحاولات + عدم إعادة الفشل غير الفريد) + تسلسل أرقام الخيط + جولة تزامن 16 كاتبًا بأرقام مميزة.
### مراجعة نقدية شاملة — شفافية التغطية (لا هجرات جديدة)
- نفاد إعادة التوليد أصبح من نوع `DocumentConflictException` → `409` برسالة «تعذر توليد رقم فريد…» تصل الواجهة في الإنتاج بدل 500 العام الصامت؛ اختبار تجاوب يضمن عدم تسريب تفاصيل القيد/المزود عبر السلسلة الداخلية.
- توسيع فضاء الرقم العشوائي من 4 إلى 5 خانات (سعة 100,000 رقم لكل بادئة/سنة) — بلا هجرة (العمود نصي بطول 50 كافٍ).
- اختبار السباق أصبح يثبت «تغيّر المرشّح» فعليًا (مستودع مسجّل بمرشحات فحص التفرّد) بدل استنتاجه ضمنيًا.
- قرار موثّق: سباق ترقيم اللاحقات المتزامن مقبول صراحةً (أثر تجميلي بلا مرجع خارجي، الترتيب الرسمي بالمعرّف، حارس نقرة مزدوجة في الواجهة)؛ العدّاد الذري مؤجَّل لأنه يستلزم هجرة لا تبرّر كلفتها.
- شفافية اختبارية: جولة التزامن «ماراثون سلامة» لا تُجبر تصادمًا حتميًا — ومسار التصادم مختبَر حتميًا في اختبارات الخدمة (تعارض حقيقي من المزود).

## [1.11.1] — 2026-09-16
### إلغاء محرر الوقوعات اليدوي (وقعة «وقوعات الملف» في صفحة التعديل)
- حذف محرر «وقوعات الملف» (`OccurrencesEditor`) من صفحة تعديل المستند نهائيًا؛ توليد الوقوعات أصبح عبر «تغيير الحالة» والعمليات النظامية حصرًا.
- إلغاء نقاط إدارة الوقوعات اليدوية الثلاث `POST/PUT/DELETE /api/documents/{id}/occurrences` — أُلغي عقد الإدارة اليدوية نهائيًا وتُستجيب النقاط بـ 404.
- حذف `UpsertOccurrenceRequest` ومرافقها من الخدمة والـ DTO والواجهة؛ قراءة الوقوعات (بطاقة/نافذة/طباعة) و«تغيير جهة» الآلي و«الاستئنافات» لم تُمَس.
- سجل الوقوعات القائم يبقى كاملًا وموسومًا `system` (لا يُعدَّل ولا يُحذف من التطبيق — إضافة وتعديل تُجرى عبر الإجراءات النظامية).
- إعادة هيكلة اختبارات الواجهة والخلفية: إزالة اختبارات الإدخال اليدوي + إضافة اختبار عقد 404 + اختبار غياب المحرر من صفحة التعديل.

## [1.11.0] — 2026-09-15
### المرحلة 5: تغيير تبعية القيود والدمج وسجل الأحداث (`docs/ENTITY_MERGE_MOVE_SPEC.md`)
- تسمية التغطية `CoverageLabel` للقيد (عرض موحّد، تحقق، هجرتان).
- سجل تغييرات الجهات `PublicEntityChangeEvents` (فهارس، `timestamptz` في Postgres) + شاشة المراقبة + تصدير.
- نقل القيود: `MoveEntry` (وضعا إعادة الربط والطيّ) و`MoveAllEntries` ضمن معاملات ذرية مع حراسة القيد الأم.
- الدمج N←1 والتوحيد وإعادة التسمية والحلول — بمرجع ثلاثي إلزامي حيث يلزم.
- مصفوفة الإشعارات الستة (رؤساء الفروع والمحامون) + وقعة «تغيير جهة» آلية لكل ملف متأثر.
### إصلاحات القبول
- تاريخ المرسوم في النقل يُحلَّل يوم/شهر (`FreeDateParser`) بدل الثقافة الثابتة — إصلاح فساد صامت.
- السرد النصي لوقعة «تغيير جهة» يصل الواجهة (`DetailsText`) ويُعرض في النافذة والبطاقة والطباعة، مع إلحاق المرجع «بموجب…» بوقعتي النقل.
